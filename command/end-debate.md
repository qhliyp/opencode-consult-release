---
description: 结束当前会诊，解除保密档位锁定，切回收敛执行模式
agent: build
---

结束当前会诊。

## 动作
1. 调用 `consult_status` 确认当前会诊状态。
2. 如有未收敛分歧，先给用户一句如实小结（未决项）再结束。
3. 调用 `consult_reset` 清空状态并解除保密档位锁定。
4. 告知用户已切回普通模式，可以重新发起 `/consult`。

## 铁律
- 只在用户明确要求结束时才执行。
- 结束前如实说明未决分歧，不遮掩。